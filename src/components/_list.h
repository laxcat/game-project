//
// Edit this enum to add components.
// The order they appear in the list will be maintained. Each item must have an 
// explicit value assigned. The enum value will be used as the permenent
// component id, used for serialization. The enum values (IDs) do not
// necessarily need to be sequential.
//

enum class Components {
    Info = 1,
    Mesh = 3,
};